{
  "name": "@local/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Utility functions and models shared between main and renderer",
  "license": "MIT",
  "author": "Chaitanya Chandurkar",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf tsconfig.tsbuildinfo dist && tsc -p tsconfig.json",
    "serve": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo \"shared package has no automated tests by design\""
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
